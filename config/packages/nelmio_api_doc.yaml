nelmio_api_doc:
  areas:
    path_patterns: # an array of regexps
      - ^/api(?!/doc$)

    documentation:
      host: '%env(MAIN_HOST)%'
      schemes: [http, https]
      info:
        title: universal counter
        description: universal counter
        version: 1.0.0